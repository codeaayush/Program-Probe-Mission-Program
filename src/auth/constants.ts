/** Demo credentials — replace with a real user store in production */
export const HARDCODED_USERNAME = 'admin';
export const HARDCODED_PASSWORD = 'admin';

/** Demo signing secret — use `process.env.JWT_SECRET` in production */
export const JWT_SECRET = 'demo-jwt-secret-change-in-production';

/** Access token lifetime in seconds */
export const JWT_EXPIRES_IN_SECONDS = 3600;
